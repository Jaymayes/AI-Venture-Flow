PORT=5000

# Cloudflare Workers AI — powers the chat assistant
# Get your Account ID from: https://dash.cloudflare.com → Overview → right sidebar
# Create an API Token at: https://dash.cloudflare.com/profile/api-tokens
#   → Use template "Workers AI - Read" or custom with AI read permissions
CF_ACCOUNT_ID=
CF_API_TOKEN=
